package Simulation;

/**
 * The Direction enum represents the possible directions of an external call.
 */
public enum Direction {

    /**
     * Represents an external call requesting an elevator to go upwards.
     */
    UP,

    /**
     * Represents an external call requesting an elevator to go downwards.
     */
    DOWN
}
